SI ALIF 07.3.4 — QUEUE AFTER SUBMIT

UX fix:
- Selecting files no longer immediately shows "0/x berhasil".
- Before submit, user only sees selected-media preview.
- Upload Queue appears only after user presses Kirim Dokumentasi / Tambahkan Bahan.
- Then progress starts from 0/x and updates normally.
- Retry behavior is unchanged.
- No Worker/API change.
