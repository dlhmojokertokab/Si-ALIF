/*
 * SI ALIF 02 runtime configuration.
 *
 * Local development:
 *   frontend : http://localhost:8080
 *   worker   : http://127.0.0.1:8787
 *
 * Setelah Worker di-deploy, ganti API_BASE_URL dengan URL workers.dev milikmu.
 */
window.SI_ALIF_CONFIG = {
  API_BASE_URL: "https://si-alif-api.sialif.workers.dev"
};
