SI ALIF 07.3.2 — SIDEBAR / BOOT BRAND SYNC

Fix:
- Sidebar brand mark no longer uses hardcoded "A ✦".
- Initial route/loading mark no longer uses hardcoded "A ✦".
- Both now use assets/icons/si-alif-favicon-192.png.
- No image asset is replaced by this patch; it uses the icon already present in the repo.
- CSS + service-worker cache bumped to 07.3.2.

Frontend only.
